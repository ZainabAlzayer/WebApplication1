@model WebApplication1.Models.InventoryModel

@{
    ViewBag.Title = "AddItems";
}

<h2>AddItems</h2>
<hr/>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="∼/Content/Styles.css" /></head>
<body>
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary()

        <p>ID: @ViewBag.ID</p>

        <p>
            @Html.LabelFor(model => model.Catogery)
                                 
           <select name="category">
         @foreach (var Eq in ViewBag.Category_List)
                     {
                    <option value = @Eq > @Eq </option >
                               }
                   
                </select>

</p>

       

        <p>Name: @Html.TextBoxFor(x => x.Name) </p>
        <p> CODE: @Html.TextBoxFor(x => x.Code)</p>
        <p>Descreption: @Html.TextBoxFor(x => x.Description
                   )</p>
        
        <input type="submit" value=" Add" />
    }
</body>
</html>